1) CRIANDO UMA CLASSE :

Uma classe é uma estrutura inicial que vamos colocar como propriedade para o nosso objeto e
o objeto é a nossa variável, imaginando e abstraindo esse conceito os objetos antes de ser
criados ela é vazia e o seu tipo é atribuído a sua classe.

2) Criando uma Mensagem inicial: Acima onde tem o def __init__(self) -> none , é o nosso metodo construtor onde essa função
é chamada quando nosso objeto é criado, então podemos colocar uma rotina inicial dentro ela